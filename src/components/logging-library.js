import React, { Component } from 'react'

const EventEmitter = require('events');
const emitter = new EventEmitter();

export class LoggingLibrary extends Component {

    render() {
        return(
            <div></div>
        );
    }
}
